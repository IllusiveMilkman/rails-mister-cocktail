<nav class="navbar navbar-light bg-light mb-4">
  <a class="navbar-brand">Cocktails 🍸</a>
  <%= link_to 'Add Doses', new_cocktail_dose_path(@cocktail) %>
  <%= link_to 'Home', cocktails_path, :class => 'nav-link' %>
</nav>

 <div class="container col-md-6 offset-md-3">
    <h2><%= @cocktail.name %></h2>
    <hr>

    <div class="container <%= @doses.count == 0 ? "hidden" : ""%>">
      <h3 style="color: green;">Ingredients</h3>
      <table class="table table-bordered">
        <thead>
          <tr>
            <th scope="col">Dose</th>
            <th scope="col">Ingredient</th>
          </tr>
        </thead>
        <tbody>
          <% @doses.each do |dose| %>
            <tr>
              <td><%= dose.description %></td>
              <td><%= dose.ingredient.name %> <%= link_to '🗑', cocktail_dose_path(params[:id], dose),
              method: :delete,
              data: { confirm: "Are you sure you want to delete this task?" } %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
</div>
